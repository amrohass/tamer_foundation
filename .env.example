# Supabase — Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only, used for seeding. Never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=

# Optional — the app degrades gracefully without it (styled list fallback instead of the map)
NEXT_PUBLIC_MAPBOX_TOKEN=

# Optional — the AI co-pilot uses cached fallback questions without it
LLM_API_KEY=
